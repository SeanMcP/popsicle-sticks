// sectionReducer.js

const initialState = {
    list: []
};

const sectionReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state; 
    }
};

export default sectionReducer;